services:
  web:
    build: .
    command: ["python", "server.py", "/app/www"]
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    volumes:
      - ./www:/app/www:ro
    restart: unless-stopped
